<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate of Completion</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
            background-color: #f8f8f8;
        }
        .certificate {
            border: 5px solid #b71c1c;
            padding: 40px;
            border-radius: 10px;
            background: #fff;
            width: 80%;
            margin: 0 auto;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .title {
            font-size: 36px;
            font-weight: bold;
        }
        .subtitle {
            font-size: 24px;
            color: #333;
        }
        .name {
            font-size: 48px;
            font-family: 'Brush Script MT', cursive;
            font-weight: bold;
            margin: 20px 0;
        }
        .description {
            margin: 20px 0;
            font-size: 16px;
            color: #666;
        }
        .signature {
            display: flex;
            justify-content: space-between;
            margin-top: 40px;
        }
        .signature div {
            width: 30%;
            text-align: center;
            border-top: 1px solid #333;
            padding-top: 10px;
            font-weight: bold;
        }
        .ribbon {
            position: relative;
            width: 100px;
            height: 100px;
            background-color: #c62828;
            border-radius: 50%;
            margin: 0 auto 20px;
        }
        .ribbon:before, .ribbon:after {
            content: '';
            position: absolute;
            width: 30px;
            height: 80px;
            background-color: #c62828;
        }
        .ribbon:before {
            transform: rotate(-20deg);
            top: 50%;
            left: -15px;
        }
        .ribbon:after {
            transform: rotate(20deg);
            top: 50%;
            right: -15px;
        }
    </style>
</head>
<body>
    <h1 style="color: #4caf50; font-size: 24px; margin-bottom: 20px; margin: auto;">This is a Valid Certificate</h1>
    <div class="certificate">
        <div class="ribbon"></div>
        <div class="title">CERTIFICATE</div>
        <div class="subtitle">Of Completion</div>
        <p>This Certificate is Presented to:</p>
        <div class="name"><%= data.name %></div>
        <div class="description">
            <p><%= data.collegeName %></p>
        </div>
        <div class="signature">
            <div>Rohit</div>

        </div>
    </div>
    <p style="font-size: 14px; color: #ff0000; margin-top: 20px;">
        This is a digital certificate only for verification and <strong>Print</strong> of this cannot be used as certificate. Please use original certificate wherever needed.
    </p>
</body>
</html>